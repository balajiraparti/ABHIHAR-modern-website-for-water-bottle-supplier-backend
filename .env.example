# ── BACKEND ENVIRONMENT VARIABLES ─────────────────────────────────────────────
#
# LOCAL DEVELOPMENT
#   Copy this file to .env and fill in your values.
#   Run the backend with: npm run dev  (starts on PORT below)
#
# VERCEL PRODUCTION DEPLOYMENT
#   In your Vercel backend project → Settings → Environment Variables, add:
#     JWT_SECRET      = <long random string>
#     ADMIN_EMAIL     = admin@gmail.com
#     ADMIN_PASSWORD  = <your admin password>
#     MONGO_URI       = mongodb+srv://...
#     DB_NAME         = abhihar
#     ALLOWED_ORIGIN  = https://<your-frontend-project>.vercel.app
#   PORT is ignored on Vercel (Vercel manages the port automatically).
# ──────────────────────────────────────────────────────────────────────────────

JWT_SECRET=your-strong-random-secret
ADMIN_EMAIL=admin@gmail.com
ADMIN_PASSWORD=Admin@123
PORT=5177

MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=Cluster0
DB_NAME=abhihar

# Local dev: use * to allow all origins
# Vercel prod: set to your exact frontend URL, e.g. https://abhihar.vercel.app
ALLOWED_ORIGIN=*
